#include <stdio.h>
/**
 *    * main - print to screen
 *       *
 *          * Return: (0)
  */
int main(void)
{
		char Mystr[] = "\"with proper grammar, but the outcome is a piece of art,";

			printf(Mystr);
				return (0);
}
