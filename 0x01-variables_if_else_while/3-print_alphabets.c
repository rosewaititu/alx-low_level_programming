#include<stdio.h>
/**
 * main-prints upper and lowr alphabet
 *
 * return:always zero
 */
int main(void)
{
char c = 'a';
char C = 'A';
for (c='a'; c <= 'z'; c++)
putchr(c);
for (C ='A';C <= 'Z' C++)
putchr(C);
putchr('\n');
return (0);
}
